spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SayedBaladoh/microservice-cours-config-server
          default-lablel: main
          clone-on-start: true

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health, info